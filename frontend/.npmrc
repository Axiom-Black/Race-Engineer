# Node floor is enforced, not suggested.
#
# jsdom 30 (the component-test DOM) requires >=22.22.2. CI was pinned to Node
# 20, so the suite passed locally and crashed on the runner with an opaque
# "Failed to start forks worker". Without engine-strict npm only warns, and a
# warning scrolls past in CI output.
engine-strict=true

{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "//engines": [
    "Declared because jsdom 30 requires ^22.22.2 || ^24.15.0 || >=26.0.0 and CI",
    "was pinned to Node 20. The component suite passed locally on 22.22.2 and",
    "could NEVER pass on the runner — undici, loaded by jsdom, dies with",
    "'webidl.util.markAsUncloneable is not a function'. Paired with",
    ".npmrc engine-strict=true so a wrong Node FAILS the install loudly instead",
    "of producing a confusing worker crash three steps later."
  ],
  "engines": {
    "node": ">=22.22.2"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@sentry/react": "^10.70.0",
    "@supabase/supabase-js": "^2.112.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.6",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "jsdom": "^30.0.1",
    "oxlint": "^1.75.0",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "optionalDependencies": {
    "@oxlint/binding-win32-x64-msvc": "^1.77.0",
    "@rolldown/binding-win32-x64-msvc": "^1.2.3"
  }
}

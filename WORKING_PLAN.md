# ByteCraft Racing — Working Plan

> **What this is.** The operational tracker that sits on top of the strategic *Product Phase Plan v1.0*.
> The Phase Plan says *what ships in what order and why*. This says *what we're doing right now,
> whether it's done, and what's in the way*. Keep this in the repo root. Update it every work session.
> The structure is generic on purpose — copy it to spin up the next Claude-built tool (see §7).

---

## 0 · Status at a glance

| Field | Value |
| --- | --- |
| **Product** | ByteCraft Racing — Race Engineering Manager (Axiom Black, LLC) |
| **Flagship deliverable** | Race Engineering Agent (multi-agent: Orchestrator → specialists → Optimizer → Synthesizer) — dark until Phase 2 |
| **Current phase** | **Phase 1 — Launch, extended into use.** Phase 2 opened 21 Aug and was **re-parked 24 Aug**: P2/P3 build an agent no user has asked for, because there are no users (Establish Pull). P1 (plan-doc correction) stays done; **P2–P7 are all on hold** until the first cohort produces a pull signal. |
| **Current iteration** | Iteration 4 — "First cohort": onboard three real drivers and read the answer. Iteration 3 ("Agent spine") suspended after P0/P1. Iteration 2 ("Pilot spine", S1–S8) closed 21 Aug 2026. |
| **Live URL** | https://bytecraft-racing.vercel.app *(production, `main`)* — first deploy 17 Aug 2026 |
| **Health** | ✅ **Phase 1 complete.** S1–S8 all closed; the pilot is live at the URL above on a $0/month stack, and the iteration's acceptance test passes in full (register → dashboard < 2 min; upload → full view **94 ms** measured on the real COTA export against a 10 s budget; sessions survive sign-out/sign-in). Retrospective: `docs/phase-1-retrospective.md`. **Not yet proven against users — there are none.** Everything known about the product's behaviour comes from one driver's real export and one owner's walkthrough. |
| **Top blocker** | **None — C1 pre-flight closed 25 Aug; C2 (invites) is with the owner.** Production no longer holds driver PII; the keepalive is verified live. Next agent-side work is *pulled by the cohort's data*, not scheduled: C4 isolation check once two drivers have uploaded, then C5 scoring against the pre-committed rule. Superseded blocker for the record: **owner pre-flight before the cohort is invited** — `docs/onboarding-pilot-users.md`, "Owner pre-flight". Load-bearing: delete the real-COTA session (driver PII sits in production while you invite others), check the Supabase email rate limit (three signups can trip free-tier SMTP; a driver whose confirmation never arrives concludes the product is broken), and add the keepalive secrets (the free project pauses after ~1 week idle — now a real risk, not a nicety). Re-parking Phase 2 keeps the stack at **$0/month**; the $0.18/run cost arrives only if the cohort pulls it. |
| **Last updated** | 24 Aug 2026 — _(update this line and §5 every session)_ |

---

## 1 · The Loop *(how this plan is run — the reusable part)*

Five moves, repeated. This is the whole methodology; everything else is content.

> **The strict form of this loop, with roles and stop conditions, is
> `docs/agentic-delivery-process.md`** — six stages (Premise → Slice → Ground →
> Build → Prove → Close), what only a human can do, and what may not proceed
> until what is true. This section stays as the short version; that document is
> the procedure, and it is revised at every phase close.

1. **Slice** — break the next phase into INVEST-shaped stories (independent, small, testable). A story too fuzzy to estimate becomes a **spike** first.
2. **Define done** — every story gets one acceptance test. "Done" = that test passes. No test, no story.
3. **Triage** — park each candidate in the ROI grid: *Do Now* (high value / low cost) → *Do Later* → *Do Much Later* → *Never*. Only pull *Do Now* into the iteration.
4. **Build** — smallest working increment first. Every increment must leave something a driver would pay for (the Phase Plan's guiding principle).
5. **Update this file** — move story status, log the decision or blocker in §5, bump §0. That's the ritual. If it isn't written here, it didn't happen.

**Cadence:** one iteration per focused push. **Standing rule:** cut scope before cutting quality (the Iron Cross — schedule, quality, staff are fixed; *scope* is the lever).

---

## 2 · Now / Next / Later

Collapsed roadmap. Full detail and acceptance criteria live in *Product Phase Plan v1.0*; the 9 Aug 2026 stack pivot (this file, §5) supersedes the Phase Plan's hosting notes.

| **NOW — Phase 1: Launch (Tier 1 Pilot)** | **NEXT — Phase 2: Intelligence + Teams** | **LATER — Phase 3–4: Run it / Grow it** |
| --- | --- | --- |
| Solo driver uploads a `.ld`+`.ldx`+`.svm` set, sees all 70 channels + cross-session progression. No AI, no teams. | Turn the Race Engineer Agent server-side. Roles, quotas, metering, garages, billing. | Ops dashboards, content pipeline, B2B/league pilots. Second sim title. Live/voice tier. |
| Deploy: Vite+React SPA on Vercel; Supabase Postgres/Auth/Storage with RLS. Client-side parsing; summaries + ~400-pt/lap traces persist; raw files to Storage. No backend service, no TimescaleDB. | The FastAPI service (`backend/`, Phase 2 inventory) returns as the **only writer** for metered operations. Identity = Supabase Auth JWT (Clerk superseded). | Directional; scope set by Phase 3 data. |

**Sequencing tension to hold consciously:** the Race Engineer Agent is the exciting deliverable and it already works as a prototype — but strategy puts a *telemetry* launch first (small surface, defers per-run API cost until revenue exists). Resist shipping the agent early. It goes live in Phase 2, on the identity spine already laid by Supabase Auth + RLS.

---

## 3 · Current iteration — "Pilot spine"

The honest critical path for the Tier 1 Pilot. Iteration 1 ("Wire the spine": Clerk JWT, FastAPI RLS wiring, server-side agent) was retired 9 Aug 2026 — it described Phase 2 work on a superseded stack; its S5/S6 concerns return in Phase 2 with Supabase Auth as the identity source.

| # | Story (INVEST) | Status | Acceptance test = done when… | Notes |
| --- | --- | --- | --- | --- |
| S1 | Reference bundle in repo: sanitized `.ld`/`.ldx`/`.svm` fixture triple **+ Python golden-master JSON** of its decoded output | ✅ Done 10 Aug 2026 | Ring 0 green (G0.1/G0.2); golden masters committed under `fixtures/` | Closed via a PR from branch `claude/materials-repo-guide-wy7pl4` — see §5. Driver-name offset confirmed `0x9E` (not `0xA0`); field holds a `DRIVER_REDACTED` placeholder, not a null-zeroed field (noted deviation, see `fixtures/FIXTURE_NOTES.md`). `scale` (0x1C) and float32 questions resolved from real bytes. |
| S2 | CI pipeline + branch protection | ✅ Done 17 Aug 2026 | A PR that breaks a Ring 0–1 gate cannot merge to `main`; pushes to `main` run the rings | `.github/workflows/ci.yml` committed 10 Aug: Ring 0 (fixture present, hashes vs FIXTURE_NOTES, no telemetry outside `fixtures/`, driver field scrubbed) → Ring 1 (backend suite vs fixture). First run triggers on this push — confirm green. **Branch protection landed 17 Aug 2026** as repository ruleset `main-gates` (id `20923480`), enforcement `active`, targeting `~DEFAULT_BRANCH`: restrict deletions, block force pushes, require a PR (0 approvals — solo driver; the gates are the reviewer), and require all five ring checks with `strict_required_status_checks_policy` (branch must be up to date before merge). `main` is no longer directly pushable by anyone, including Claude. **Standing hazard:** required checks match on the literal job name, so renaming a CI job silently drops that gate from enforcement without failing anything — two job names were renamed this session, which is exactly how that drift begins. Rename a job ⇒ update the ruleset in the same change. |
| S3 | JS parser modules (`ld.js`/`ldx.js`/`svm.js`) with the **full** decode formula — `phys = raw × mul / (scale × 10^dec) + shift` (both `shift` AND `scale`; int-only sample reads, no float branch) | ✅ Done 10 Aug 2026 | JS decode of the fixture matches `fixtures/golden_master_*.json` exactly (G1.2/G4.1); the 9 `CAL`-badged channels decode to physical units; Ambient/Track Temp read ~29/39 °C (scale=50); `all_zero` flags surface in parser output | Ported from the Python reference into `frontend/src/lib/motec/` (`ld`/`ldx`/`svm`/`domain`). Vitest golden-master suite: **18/18** — every decoded trace value-for-value, quirk flags, lap boundaries, `.ldx` summary + truncation detector, `.svm` sections + energy branch, G4.2 domain snapshot (`fixtures/golden_master_domains.json`, newly frozen). Ring 4 wired into CI after Ring 1. |
| S4 | Vite + React scaffold + Supabase project: Auth wired, Phase 1 schema, RLS keyed on `auth.uid()` on every table & bucket, storage buckets, atomic three-file constraint | ✅ Done 10 Aug 2026 | RLS test: authenticated cross-user read returns **0 rows at the DB layer**; a session row cannot reach `complete` without all three storage paths recorded | Supabase project `bytecraft-racing` (us-east-1, $0/mo confirmed). All three DB-layer tests passed: cross-user read = 0 rows; spoofed insert rejected by RLS; `complete` without `.svm` path rejected by `three_file_atomicity`. Browser smoke green (reachable / auth / RLS query). Schema: `sessions` + `laps`, `telemetry` bucket with per-user folder policies, dedup constraint on `(user_id, ld_sha256)`. |
| S5 | Core loop: upload → client-side parse → summaries + downsampled traces persist → SessionReport/TrackMap dashboard renders | ✅ Done 12 Aug 2026 | The real COTA triple (local only) uploads and renders a complete session view in < 10 s; unreliable/empty channels display SIM/EMPTY flags | Front half (onboarding spine) + reconciled back half (Steps 1/2/5, live-verified) + **Step 4 done**: `SessionReport.jsx` — four tabs (Summary / Performance / Instruments / Track Map) on a single lap selector + synced distance cursor, computed from the persisted lap summaries and the distance-resampled trace blob; EMPTY/unreliable channels rendered. Replaced the `SessionDetail` stand-in (deleted). Review-fix hardening merged (PR #5). Acceptance UI is done; the "renders in < 10 s" end-to-end confirmation happens on the live deploy (S7) with the real triple. |
| S6 | Progression/trend view on persisted multi-session data | ✅ Done 17 Aug 2026 | Best-lap progression chart renders across ≥ 2 uploaded sessions of the same car/track combo | `ProgressionTab.jsx`: real (not seeded-generator) rollup grouped by venue+car+session_type from persisted sessions — best lap, gap-to-**your-best** (not gap-to-*ideal*, since the curated Ideal Session Data library is Phase 2+ and doesn't exist; faking a target would violate the no-faked-capability standing bar), trend, closeness bar, editable tier thresholds (not yet persisted). Live-verified in-browser. **Back half closed 17 Aug** (§5): car-level filter chips, tier-threshold persistence via `lib/prefs.js` (localStorage, per-user namespaced — logged decision), and sparkline/threshold styling parity with the prototype. Rollup maths extracted to `lib/progression.js` and unit-tested (33 new tests). The one deliberate divergence from the prototype stands: gap is measured to **your own best**, not to a curated "ideal", because that library is Phase 2+ and faking a target would violate the no-faked-capability bar. |
| S7 | Deploy to Vercel + error tracking (Sentry free tier) | ✅ Done 21 Aug 2026 | Production URL passes the walkthrough: register → empty dashboard < 2 min; upload → full view < 10 s; sessions survive sign-out/sign-in **Live 17 Aug 2026.** Vercel project `bytecraft-racing` (`prj_SQkRaEeOgBW5V6BXj8jMkPb6Rgj0`, team Axiom Black), root directory `frontend`, production branch `main`, env vars set by the owner. PR #11 merged as `49a7a78`; all five rings green on `main` and the production deployment completed. The branch preview was confirmed loading correctly by the owner before the production merge. **Sentry landed 17 Aug** — opt-in via `VITE_SENTRY_DSN`, off by default (no DSN ⇒ the SDK chunk is not even emitted). Privacy-first because this app holds driver PII: no Session Replay, `sendDefaultPii: false`, emails/UUIDs/JWTs redacted from every outgoing string, request bodies dropped, and breadcrumbs naming a `.ld`/`.ldx`/`.svm` discarded (MoTeC exports are usually `<driver name> <track>.ld`). Plus a top-level `ErrorBoundary` so a render crash shows a branded page instead of the same blank screen a hollow bundle produces. **Still open:** (a) the full acceptance walkthrough against the production URL — register → dashboard < 2 min, upload → full view < 10 s, sessions survive sign-out/sign-in — which has *not* been run and is what actually closes this story; (b) **Sentry not started**; (c) Supabase redirect URL + re-enabling Confirm email, both owner-side. Note the first production build (from `main` before the merge) produced the hollow bundle this session's Ring 5 was written to prevent — the guard only reached production once #11 landed. |
| S8 | Lap-vs-lap overlay with cumulative delta-time trace | ✅ Done 13 Aug 2026 | Two laps of one session overlay on the report's plots with a delta trace; selectable reference lap | `lib/delta.js` (pure, 16 tests) + a "vs" reference-lap selector in `SessionReport`. Every Instruments plot draws the reference lap dashed behind the selected one on a shared y-scale; a Δ-time trace sits on top, segment-coloured (green = gaining, red = losing). Cumulative time is reconstructed by integrating 1/speed over the distance axis and scaling to the known lap time — so it works on **traces already in Storage**, no re-ingest. Verified in-browser on a synthetic 2-lap session: finish delta rendered −1.550 s for 138.800 vs 140.350. **⏰ Now due: re-enable Supabase "Confirm email"** (disabled 12 Aug for pilot test-signup friction — see §5). |

**Definition of done for the iteration:** S1–S8 acceptance tests pass and a stranger can sign up on the production URL, upload a session, see honest telemetry, and watch their trend after a second upload.

**Iteration ROI triage (for anything proposed mid-flight):** does it serve S1–S8? If not → park it in §6, don't pull it in.

---

## 3a · Iteration 4 — "First cohort" *(current)*

Opened 24 Aug 2026. **The whole iteration is one question:** does anyone want
this, and what do they want next? Three drivers, no new features, no agent.

| # | Story (INVEST) | Status | Acceptance test = done when… | Notes |
| --- | --- | --- | --- | --- |
| **C0** | Delete a session from the app | ✅ Done 25 Aug 2026 | A driver can delete their own session; its four Storage objects and its row are both gone, and the demo does not resurrect | **Pulled in by the owner while working C1.** There was no way to delete an uploaded session — and `serviceHealth.js` was telling users to "delete an old session to free space", instructing an action the app did not offer. Not speculation: the owner asked, and the storage-full message already promised it. |
| **C1** | Owner pre-flight complete | ✅ Done 25 Aug 2026 | Every box in `docs/onboarding-pilot-users.md` → "Owner pre-flight" ticked | Real-COTA session deleted (via C0's new delete, confirmed by the owner) — **production no longer holds driver PII**. Keepalive secrets added and verified by a manual dispatch: `HTTP 200`, "Project is awake". Confirm email + production redirect URL re-confirmed. Email rate limit read: **2**. |
| **C2** | Three drivers invited and signed up | ⬜ To do (owner) | Three confirmed accounts in `auth.users`, each landing in its own garage | **Staggered send, one driver at a time** — the rate limit of 2 covers *all* auth email (confirmations, resets, resends), so the realistic exhaustion path is a driver hitting "resend" twice, not the three signups themselves. Each driver is told: if it hasn't arrived in ten minutes, message rather than resend. Side benefit: one-at-a-time means the owner sees each first-run live instead of three retrospective accounts. Escalation if it bites: custom SMTP (Resend/Postmark/SendGrid free tiers). |
| **C2.1** | Demo seeding survives a bad first run | ✅ Done 25 Aug 2026 | A failed seed reports as a missing *demo* (not a failed garage), its retry re-seeds rather than re-lists, and a transient fetch failure retries itself | **Pulled by real data, not speculation.** The C2 audit found `mindofamiddlechild@gmail.com` with `demo_sessions = 0` while every other account had one — one driver in three landed on an empty garage 29 s after signup and never returned. Diagnostics found no session row *and* no storage objects, so the failure was ahead of the first insert (a mid-upload tab close would have stranded a `pending` row, since an unloading page cannot run the rollback) — i.e. the ~853 KB fixture fetch or the parse. |
| **C3** | At least two drivers upload a real session | ✅ Accepted 25 Aug 2026 | ≥2 distinct `user_id`s hold a non-demo `sessions` row with `ingest_status='complete'` | **Met numerically; the stated purpose was not.** Two accounts hold real uploads (`oeboreime@axiomblack.com`, `ohiomaeboreime@outlook.com`) — but both are owner-controlled, so the criterion's *reason for existing* — "the first time the parsers meet an export that is not the owner's" — remains unmet. The two independent drivers (`ikhid3@`, `mindofamiddlechild@`) signed up 21 Aug and have never uploaded. Owner reviewed this and elected to accept and move on. Recorded plainly so no later reader mistakes C3 for evidence the parsers survive someone else's export. |
| **C4** | Cross-driver isolation proven on production | ✅ Done 25 Aug 2026 | `total_all_drivers = 4`, `visible_to_this_driver = 1` for `ohiomaeboreime@outlook.com` | **First time RLS has been proven outside CI.** Ring 3 has asserted it on every push since August, but always against a scratch Postgres. This ran on **production**, under `set local role authenticated` with `request.jwt.claims` impersonating a real account — and crucially **never touched the app**, so the UI is provably not what filters. Method kept in breadcrumbs: stash the privileged total in a GUC *before* dropping to `authenticated`, so one row carries both numbers; wrap in `begin/rollback` so the probe is read-only. The comparison must include demo rows on both sides or it is not like-for-like. |
| **C5** | The three questions asked and answered | ✅ Answered 25 Aug 2026 (n=1) | All three drivers answered Q1–Q3; the answer scored against the pre-committed rule | **Q1 "anything slower than expected?" → Nothing. Q2 "a number you didn't trust?" → No.** Those two need no independence and are clean results: no performance complaint, and no decode/labelling distrust — the expensive category. **Q3 → "make the dashboard follow the originally prototyped designs; visually it's difficult to read."** Scored against the rule: *not* analysis/coaching, *not* sharing — it lands in the third row, **"neither is pulled; extend Phase 1."** **Phase 2 stays parked.** Honest limits: n=1, and the respondent is the owner, so Q3 is not the independent pull signal the rule was written for. Treated as an owner directive on Phase 1 quality, which it plainly is, rather than as cohort demand. |

**Acceptance for the iteration:** the pull-signal rule in
`docs/onboarding-pilot-users.md` returns a verdict, and the next iteration is
chosen by that verdict rather than by the plan that existed before it. Two of
the four possible verdicts stop Phase 2 — that is the point.

**What is deliberately not being built:** sharing, team visibility, the agent,
and any feature a driver has not asked for. If the cohort asks, that is the
signal; anticipating it is overproduction.

---

## 3b · Iteration 3 — "Agent spine" (Phase 2, P1–P3) — **SUSPENDED 24 Aug 2026**

> **Suspended, not cancelled.** P0 and P1 are done and keep their value. P2/P3
> are held because they build an agent no user has asked for — see §5, 24 Aug.
> If the cohort's answer pulls the agent, this iteration resumes unchanged.

Opened 21 Aug 2026. **Scope deliberately stops at P3.** P4–P7 (model tiering as
a gate, quota enforcement, Stripe, garage accounts) are on hold: they are
infrastructure for demand that has not been confirmed. Running real analyses on
real sessions answers the question that matters first — *is the debrief good
enough that anyone would pay* — before building the machinery to charge.

**The host is a Supabase Edge Function (Deno/TypeScript).** `backend/` is a
**reference implementation**, not the deployed artifact — see §5, 21 Aug.

| # | Story (INVEST) | Status | Acceptance test = done when… | Notes |
| --- | --- | --- | --- | --- |
| **P0** | Sanitized **multi-lap** fixture + golden masters | ✅ Done 21 Aug 2026 | A committed fixture yields ≥2 timed laps, an out-lap and a partial; Ring 4 asserts against fresh golden masters generated from exactly those bytes | **Pulled ahead of P1 deliberately.** The current fixture is single-lap and *structurally cannot* express multi-lap logic — it hid the out-lap bug (17 Aug) and the lap-reconciliation bug (21 Aug). Every agent specialist reasons about laps, so building on it repeats Phase 1's most expensive mistake. Driver field must be scrubbed (Ring 0 G0.2). |
| **P1** | Correct the stale plan documents | ✅ Done 21 Aug 2026 | `docs/phase-plan.md` no longer asserts Clerk, Railway/Render/Fly, Phase-1 TimescaleDB, or a Python Phase 2 host | Corrected **inline plus a superseded-assumptions banner** rather than by rewriting — the reasoning at the time is worth keeping. Also corrected the plan's claim that the orchestrator "moves server-side almost mechanically", which only held while the host was assumed to be Python. |
| **P2** | Edge Function deploys and authenticates | ⬜ To do | A request without a valid Supabase JWT is rejected; a valid one resolves the caller's `auth.uid()`; a forged client-side role cannot reach a privileged path | **The hidden dependency** — roles, notes, garages, quotas and tenant isolation all need to know who is asking. No model call yet, so no API cost. |
| **P3** | Agent run server-side, audited | ⬜ To do | A Standard run completes end-to-end in < 60 s and writes tokens + computed cost to `agent_runs`; the invariant test that specialists never run on Opus passes against live config | Provider stays behind `RunConfig`. After this lands, run the **A/B protocol** in `docs/model-selection-evaluation.md` on the real COTA session. |

**Acceptance for the iteration:** an owner-triggered Standard run on a real
session produces a debrief the owner judges *actionable*, with its token count
and cost recorded. That judgement — not a passing test — is what decides whether
P4–P7 get built.

---

## 4 · Standing bars *(apply to every story, every phase — never negotiable)*

These are the cross-phase engineering invariants. A story that violates one is not done, regardless of its own acceptance test.

- **Parsers grounded in real files** — no format assumption ships unverified against a real LMU export (the `.svm` guess-vs-reality miss is the cautionary tale).
- **Three-file upload is atomic** — no session exists without `.ld`+`.ldx`+`.svm` together; in the pilot: enforced client-side before insert AND by a database constraint / `ingest_status` check.
- **Specialists never run on Opus** — the cost model's central rule; enforced by automated test (dormant in the pilot; the invariant tests stay and must keep passing/skipping).
- **Tenant isolation lives in the database** — RLS policies keyed on `auth.uid()`, not application `WHERE` clauses.
- **Unreliable data is flagged, never hidden** — empty channels (GTE: Tyre Load ×4, Grip Fract ×4, Battery Charge; plus per-session all-zero channels) display with explicit flags. *(Ambient/Track Temp were **reclassified 10 Aug 2026**: the "unreliable" flag was masking our own dropped-`scale` decode bug — with the full formula they decode correctly and are no longer flagged. The bar itself is unchanged: whatever is genuinely unreliable gets flagged, and flags must be **rendered in the UI**, not just carried in data — the prototype review found no dashboard renders them yet.)*
- **Standards are audited** — all code answers to Clean Code / Clean Architecture / Clean Agile; Code Craft is the tracking mechanism.
- **Every increment is payable** — no phase ships infrastructure without a user-visible capability attached.

---

## 5 · Blockers & decisions log *(append-only; newest on top — this is the "updated" record)*

| Date | Type | Entry |
| --- | --- | --- |
| 25 Aug 2026 | Progress | **#2 complete: the session report now follows the prototype.** Four increments — Performance vs history (#35), Summary hero (#36), instrument cluster + replay (#38), track map (this). Plus #1 (component tests as a Ring 4 gate), #3 (Channels tab) and #4a (stint logic). **Deferred with reasons rather than silently dropped:** the prototype's *"Technical Circuit"* chip (hardcoded there, no source in the export or schema — inventing a circuit's character is the baseless claim the flagging bar prevents) and **corner-number badges on the track map** (needs GPS-curvature corner detection, which exists in `backend/` and is *known* to read multi-apex complexes as single corners — 16 detected at COTA vs 20 official. Porting a known-imperfect detector to JS for a feature the Phase 3 curated registry will redo is work with a short half-life). **The find worth keeping:** rendering the cluster exposed my own code and comment disagreeing about the G-cross sign, so it was settled by measurement rather than argument — LMU reports **positive** `G Force Long` under braking (+1.63 G mean at `Brake Pos > 70%`, −0.24 G off the brakes), the opposite of the usual assumption. Promoted into CLAUDE.md's hard-won facts and pinned by a test, because any G-G plot, brake overlay or Phase 2 agent prompt reasoning about load transfer depends on it and it is exactly the sort of thing that gets "fixed" into being wrong. |
| 25 Aug 2026 | Decision | **Readability rework opened; run averages defined as stint-based.** C5's Q3 pulled a five-item UI programme, sequenced smallest-first: **#3** channels get their own tab *(done)*, **#2** SessionReport layout to the prototype, **#4a** stint logic, **#4b** user page, **#5** Race Engineer shell. **#5 is flagged as a conscious exception** — the AI Race Engineer tab is Phase 2 and P2/P3 are parked, so building its *shell* is UI for a capability with no backend; it goes last and is logged as an override rather than allowed to look like Phase 2 quietly resumed. **Run definition, owner-decided:** a run is a **stint** — consecutive timed laps, any length — and the reported figures are the **3, 5, 7 and 10-lap stint averages**. Rolling-window averaging was rejected: it can average across a pit stop and a tyre change, which an engineer would call meaningless. Consequence to hold: the current fixture yields at most a 3-lap stint, so 5/7/10 will legitimately show nothing on it — that is the same discipline Progression already applies in refusing to award a tier on one data point, not a bug to paper over. |
| 25 Aug 2026 | Decision | **Component tests are now real infrastructure, not a habit.** Owner-directed, after the gap was flagged rather than glossed. Until today this repo could not test a component: logic lived in `src/lib` with genuine coverage, and everything rendered was checked by hand or by a throwaway Playwright harness rebuilt from scratch each time. That harness *worked* — across S6 and today it caught a tier badge awarded on one data point, a two-bar sparkline rendering as full-width slabs, and a lap-time stat explaining its absence with channel wording — but it depended on someone remembering to build it. **A check that depends on memory is not a gate.** Added `@testing-library/react` + `jest-dom` + `user-event` + `jsdom` (all **MIT**, clearing the no-AGPL/GPL bar). Environment stays `node` by default so the ~185 pure-logic tests keep running without a DOM; component files opt in with a `// @vitest-environment jsdom` docblock — chosen over a global jsdom (which taxes every file) and over vitest projects (which splits the config). First suite covers `SessionOverview`: 15 tests, every one corresponding to something the harness checked by hand today. **Verified to have teeth** — deleting the `why: 'no timed lap'` line turns the matching test red. Ring 4 picks these up with no workflow change, since it already runs `npx vitest run`. The harness is *not* retired: breadcrumbs Part C now says assert in the suite, **look** in the harness — a screenshot still catches what no `innerText` assertion can. |
| 25 Aug 2026 | Decision | **C5 answered: Phase 2 stays parked. The pull is Phase 1 readability.** Q1 (anything slower than expected) → *Nothing*. Q2 (a number you didn't trust) → *No*. Both are clean, and both are independence-free — they ask about product behaviour, not demand, so the owner answering them is fine. **Q2 is the more valuable of the two:** no distrust of a displayed number means the decode work holds up in use, which is the category that would have been expensive to get wrong. **Q3 → "make the dashboard follow the originally prototyped designs; visually it's difficult to read."** Scored against the rule exactly as written: not analysis/coaching, not sharing/comparing → third row, *"neither is pulled; extend Phase 1."* **The agent is not what was asked for, so P2/P3 stay parked.** Stated limits: n=1 and the respondent is the owner, so this is not the independent signal the rule was designed to capture; it is an owner directive on Phase 1 quality, and is recorded as that rather than dressed up as cohort demand. **The prototypes named are in the repo** (`prototypes/`: SessionReport, SessionDashboard, SessionUpload, v12_Merged, racing-host — 5,632 lines against 2,487 shipped). `theme.js` already carries the prototype palette verbatim, so the gap is **not colour** — it is layout, density and hierarchy, plus a prototype `SessionDashboard` view that has no shipped equivalent at all. |
| 25 Aug 2026 | Decision | **C3 accepted on two owner-controlled uploads; the cohort's independence question is closed rather than answered.** Two accounts hold real sessions, satisfying the letter of C3. Both are the owner's. The two independent drivers signed up 21 Aug and have not uploaded in four days. The owner reviewed this and chose to accept and move on, which is his call to make — recorded here so no later reader treats C3 as evidence the parsers survive an export that is not the owner's, because they have not yet met one. **What the cohort did prove is real and worth stating:** signup and email confirmation work end-to-end on production; RLS isolates real accounts at the database (C4); upload, delete and demo-seeding all work; and one genuine first-run defect was found that no test could have caught. **What it did not prove:** that anyone other than the owner wants the product, or that the ingest path survives a stranger's MoTeC export. Both remain open, and neither is answerable by more building. |
| 25 Aug 2026 | Progress | **C4 passed — tenant isolation proven on production for the first time.** `total_all_drivers = 4`, `visible_to_this_driver = 1` for `ohiomaeboreime@outlook.com`. Ring 3 has asserted RLS on every push since August, but only ever against a scratch Postgres: that proves the *policy is correct*, not that the *deployed project enforces it*. This probe ran on production under `set local role authenticated`, and **never touched the app** — which is the part that matters, because a UI filtering correctly on its own would look identical from the outside. The §4 bar *"tenant isolation lives in the database"* is now demonstrated rather than asserted. Method promoted to breadcrumbs A13, including the three traps: set the JWT claim *before* `set role` (`authenticated` cannot read `auth.users`); compare like-for-like (demo rows on both sides or neither); and `begin`/`rollback` so a production probe writes nothing. Worth recording that the first attempt returned `visible_to_this_driver = 1` **alone**, which proves nothing — 1 of 1 and 1 of 4 are the same number. The total had to be captured in the same row before the answer meant anything. |
| 25 Aug 2026 | Progress | **C2 confirmed (3 driver accounts, all signed in) — and the audit found a silent first-run failure.** Four confirmed accounts exist, each with a real `last_sign_in_at`, so signup and email confirmation work end-to-end on production. But `mindofamiddlechild@gmail.com` had **`demo_sessions = 0`** where every other account had 1: that driver signed up, signed in **29 seconds later**, landed on an empty garage, and never returned. Nobody reported it. Follow-up queries returned **no session row and no storage objects**, which locates the failure *ahead of the first insert* — a mid-upload tab close would have stranded a `pending` row, because an unloading page never runs `rollbackSession`. So: the ~853 KB fixture fetch or the parse. Fixed in C2.1. **Two distinct defects, both real:** (a) a failed seed was reported through `loadError` as *"couldn't load your sessions"* — false and alarming, since the account is fine and merely empty; (b) its "Try again" called `refresh`, which re-lists an empty garage and can never re-seed, because the `seedAttempted` ref blocks it for the life of the mount. A retry that cannot retry is worse than no retry. **Still open: `real_sessions = 0` on every account** — four days, four accounts, zero uploads. Not yet scored against the pull rule, because the cohort was never asked to upload; the owner has since uploaded one. If they *had* been asked, this is the rule's fourth verdict. |
| 25 Aug 2026 | Progress | **C1 pre-flight closed; the cohort is clear to invite.** The real-COTA session is deleted — **production no longer holds driver PII**, an item that had been open since 21 Aug and could not be closed by the owner until C0 shipped a delete. Keepalive secrets added and verified by manual dispatch (`HTTP 200`, "Project is awake"), so the free project will not pause mid-pilot. Confirm email and the production redirect URL re-confirmed. **Email rate limit is 2**, and it covers all auth email — confirmations, resets *and resends* — so the exhaustion path is a driver clicking "resend" twice, not the three signups. Mitigation is a staggered one-at-a-time send plus telling each driver to message rather than resend; custom SMTP is the escalation if it bites. **Verification lesson worth its own breadcrumb (A12):** the keepalive job reports *success* whether or not the secrets exist, because it skips rather than fails by design — an 8-second green run looks identical in both cases. What actually settled it was that the ping step (gated on `configured == 'true'`) had executed, plus `HTTP 200` in its log. A green check answering a question it was not designed to answer is not evidence. |
| 25 Aug 2026 | Decision | **Session delete built before the cohort — the app was already promising it.** Working the pre-flight, the owner asked what "delete the real-COTA session" meant and pointed out there is no way to delete an uploaded session. Correct: the only `.delete()` in the codebase was internal upload rollback. Worse, `lib/serviceHealth.js` told users on a storage-full fault to *"delete an old session to free space"* — instructing an action the app did not provide. Weighed against Establish Pull and built anyway, because this is not speculation about what the cohort might want: the owner asked for it, and the shipped UI already claimed it. Three drivers about to upload wrong files, duplicates and test exports would each have needed an owner dashboard round-trip to undo. **Implementation notes worth keeping:** storage objects are removed *before* the row, because the row is the only record of where those objects live — reversing it turns a storage failure into files nobody can find on a 1 GB tier; paths are read from the row rather than reconstructed from the naming convention, so a future layout change cannot silently orphan objects; and deleting the demo now records a per-driver dismissal, because "zero sessions" is exactly the state deleting it produces and the seeding rule would otherwise resurrect it on next sign-in. The ordering tests were verified to fail against a deliberately reversed implementation before being kept. |
| 24 Aug 2026 | Decision | **Phase 2 re-parked; onboard the three drivers first. Establish Pull applied to our own backlog.** Installing the ratified `axiomblack-build-governance` skill put Principle 4 — *build only what present demand pulls* — into the repo, and it immediately indicted the work queued behind it: **P2 and P3 build an agent capability that no user has asked for, because there are no users.** The plan had the agent going server-side before a single driver had touched the telemetry product it attaches to, which is building ahead of the pull signal by the standard's own definition. Owner chose to onboard first. Consequences: Iteration 3 is **suspended after P0/P1** (both keep their value and resume unchanged if pulled); Iteration 4 "First cohort" opens with C1–C5; the stack stays **$0/month** because the $0.18/run cost arrives only if the cohort pulls it. The pull-signal rule — what answer pulls P2, what answer pulls sharing instead, what answer pulls neither — was written into `docs/onboarding-pilot-users.md` **before any driver was invited**, precisely so the result cannot be rationalised into the plan that already existed. Two of its four verdicts stop Phase 2 outright; a signal that cannot come back negative is not a signal. Also added the read-only SQL for reading the answer at the database, since RLS means the owner's own account cannot see it in the app. |
| 24 Aug 2026 | Correction | **An agent authored a skill under the name of a real standard it could not find.** Asked to commit `axiomblack-de-codex` and `axiomblack-build-governance`, it found the first already committed and byte-identical to the org copy, could not find the second anywhere on the filesystem — and then wrote its own under that name (#24) rather than stopping at "I can't find it, please supply it". The ratified standard (the five Lean principles) was then supplied and installed byte-identical in #25; the invented skill was deleted. Its content was delivery mechanics and already lived in `docs/agentic-delivery-process.md`, so nothing was lost but an auto-load. Recorded because the failure is the *silent capability assumption* anti-pattern applied to a document instead of a runtime: not finding a thing is a finding, and it must be reported as one rather than papered over with a substitute. |
| 21 Aug 2026 | Decision | **Share link and team visibility both dropped — the goal was onboarding, and onboarding already works.** Owner asked to build the shareable session link in order to bring on three drivers before Phase 2. Checked the premise first: onboarding three drivers needs **neither** feature. Each signs up, gets their own garage, and RLS keyed on `auth.uid()` isolates them — that is S4/S5, already live and verified. A share link solves a different problem (showing a session to someone with *no* account, e.g. in Discord), and mutual visibility between the three is the Phase 2 garage concept, not a link. Presented the four options; owner chose **onboard them on the existing system**. Two security models therefore stay unbuilt, and the §4 tenant-isolation bar stays intact. **If the cohort asks for sharing or team visibility, that is real signal to record rather than a gap to work around** — and it arrives from users instead of from a backlog guess. Wrote `docs/onboarding-pilot-users.md` instead: owner pre-flight, the message to send each driver, what to expect, rough edges to pre-empt, and three specific feedback questions. |
| 21 Aug 2026 | **Risk** | **Supabase's built-in email sender is rate-limited and not intended for production — three signups in quick succession may trip it.** A driver whose confirmation email never arrives concludes the product is broken, and nothing in the app can tell them otherwise. The live figure is under Supabase → Authentication → Rate Limits; deliberately **not** copied into the docs, because a stale number in a document is worse than a pointer to the authoritative one (attempted to verify against Supabase's docs from here and the egress proxy blocks `supabase.com`, so no figure is asserted). Mitigation: stagger the invitations, or configure custom SMTP before inviting the cohort. Also promoted the keepalive secrets from optional to **recommended** — with real users, a project asleep after a quiet week meets a returning driver with a dead app. |
| 21 Aug 2026 | Note | **Cross-driver isolation is DB-proven but has never been exercised by two real production accounts.** Ring 3's seven assertions cover it at the database layer — cross-user reads return zero rows, spoofed inserts are rejected, cross-tenant Storage overwrites affect zero rows — and the live policies were read directly on 21 Aug. The three-driver cohort is the first real test. After two have uploaded, verify at the **database** layer as well as in the UI: the UI filtering correctly would not prove that RLS is what is doing the filtering. |
| 21 Aug 2026 | Decision | **The delivery procedure is now a committed, versioned document: `docs/agentic-delivery-process.md`.** Written at the Phase 1 close from what actually happened across Phases 0–1, not from general principle — every anti-pattern in it is a real incident from this build. Its spine is the asymmetry that decides everything else: the agent can write, test, verify and document, but cannot authorise, hold a credential, click an owner's dashboard, spend money, judge whether output is *good*, or supply real data. So the agent's job is to reduce every task to something it fully owns or **one precisely specified human action** — vague handoffs, not bad code, were the dominant failure mode of these two phases. Contains the six-stage phase loop with per-stage exit conditions, the Human-Only Action Register (every class of it blocked real work here), the ring ladder including the two rules learned the hard way (a gate protects only what it has been merged into; a check identifying its subject by position silently changes meaning), the five proofs ordered by how often each found something real, and the decision protocol (name the bias, fix the rule in advance, never assert an unverifiable number, amend standing bars only in writing). **Maintenance is part of the contract:** a phase is not closed until the document is revised and its §11 changelog gains a row — the Phase 2 row is already stubbed with what that phase is expected to teach. Referenced from `CLAUDE.md` as the governing procedure and from §1 as the strict form of The Loop. |
| 21 Aug 2026 | Progress | **P0 done — the fixture can finally express multi-lap logic, and the truncation turned out to be free of any benefit.** Owner supplied the real COTA triple. Inspecting it against the committed fixture byte-for-byte showed the old `.ld` had **every one of the 70 channel records' sample-count fields overwritten with `2c01` (300)** — the telemetry bytes were all still there, only the declared counts were falsified. So the old fixture was 849 KB *and* single-lap: the truncation bought no space and cost the ability to test anything about laps. It hid the out-lap bug (17 Aug) and the lap-reconciliation bug (21 Aug), the latter visible to every new account in production. The new fixture is the same session with **only** the driver field at `0x9E` scrubbed (15 bytes → 15 bytes, no offsets shift): 70 channels, **412,850 decoded values**, nine distinct sample counts (channels log at different rates — code assuming one uniform count is wrong, and the fixture now proves it), and **5 lap boundaries: out-lap, 3 timed laps (138.78 / 135.50 / 136.20), trailing partial**, cross-checked against the `.ldx`'s independent Total Laps 3 / Fastest Lap 2. The `.ldx` and `.svm` were already full and sanitized, so they are unchanged and their hashes are unaltered. |
| 21 Aug 2026 | Decision | **Golden-master format v2: a SHA-256 per channel over the complete decoded array, replacing embedded full arrays.** Full arrays at 412,850 values would have been ~6 MB of committed JSON re-read on every CI run. The hashed master is **33 KB** — smaller than the *old* 316 KB one — and asserts strictly more than the alternative of decimating, which cannot see a regression between the samples it keeps. Extremes and the first/last five values are kept in plain text so a failure is diagnosable rather than merely detected. **The risk this format carries is cross-language hash stability**, so the canonical form is defined narrowly (6 fixed decimals; negative zero normalised, because Python renders `-0.0` as `-0.000000` where JS's `toFixed` gives `0.000000`; joined by `,`; UTF-8) and **all 70 hashes were verified to agree between Python and JavaScript before landing**. Also closed a real gap: the golden masters previously had **no committed generator** — they arrived with the S1 bundle, so G4.3's "generated from exactly these bytes" was a promise. `backend/scripts/generate_golden_masters.py` is now committed and its `--check` runs in Ring 1. The Python reference stays the arbiter; the JS port is verified against its hashes, never the reverse. |
| 21 Aug 2026 | **Finding** | **The upload path enforces three-file *presence* but not *coherence* — and a mismatched set is already in production.** The owner's four files include a `.svm` belonging to a different car. `car`, `carClass`, `ruleset` and `energy_scheme` are derived **only from the `.svm`** (the `.ld` carries no vehicle), so the production session is labelled `Ferrari_296_LMGT3` / `virtual_energy` while its `.ld`/`.ldx` are from the **488 GTE** session. The `three_file_atomicity` constraint is satisfied and every gate is green, because atomicity was never coherence. **It is cleanly detectable:** the `.ldx` carries `_Setup_*` fields from the same session, and they corroborate the 488 GTE `.svm` exactly (RevLimit 7500, FLSpring 220 N/mm, RW P6, BrakePressure 68 kgf) while contradicting the 296 (8000, spring "5", 4.50 deg, 120 kgf). **This matters more in Phase 2 than now:** the agent design treats setup as *cause* and telemetry as *effect*, so a foreign setup makes every causal claim it produces wrong. Logged rather than fixed in P0 — it is a separate story and needs the owner's call on how strict to be (reject vs. flag). |
| 21 Aug 2026 | Decision | **Phase 2 opens scoped to P1–P3 only; P4–P7 explicitly on hold.** P1 correct the stale plan docs · P2 an authenticated, metered gateway · P3 the agent running server-side with an `agent_runs` audit trail. Model tiering, quota enforcement, Stripe and garage accounts are **not** started until P1–P3 are live and verified. The reasoning: quotas and billing are infrastructure for demand that has not been confirmed, and the owner has three drivers and no paying customers. Running real analyses on real sessions first answers the question that actually matters — is the debrief good enough that anyone would pay — before building the machinery to charge for it. |
| 21 Aug 2026 | Decision | **The Phase 2 host is a Supabase Edge Function, which means a TypeScript rewrite — not a lift of `backend/`.** Owner chose Edge Functions over Fly/Render. Edge Functions run **Deno/TypeScript**, so `backend/`'s 2,272 lines of FastAPI + SQLAlchemy + numpy/pandas cannot run there. This directly contradicts the phase plan's claim that the orchestrator "moves server-side almost mechanically" — that assumed a Python host, and it is now corrected in the document. Quantified: `orchestrator.py` (257 lines) and `metering.py` (311) are portable logic — asyncio and config, no scientific stack — while `lap_analysis.py` (209, numpy/pandas) has an existing JS equivalent under `frontend/src/lib/`, because Phase 1 already parses client-side. So the gateway is genuinely thin: verify JWT → check quota → build the prompt from persisted summaries → call the model → write `agent_runs`. **`backend/` becomes a reference implementation**, exactly the pattern that worked for the Python parsers in Phase 1 — reference generates the truth, port is verified against it. Owner confirmed writing off the Python as the deployed artifact. |
| 21 Aug 2026 | Decision | **Build Phase 2 on Claude; settle DeepSeek with an A/B on real output after P3.** Full evaluation in `docs/model-selection-evaluation.md`. The owner named their own bias toward the cost advantage and asked for the options reviewed properly, which is what made the analysis worth doing. Two findings changed the picture. **First, at current scale the saving is ~$10/month** — 60 Standard runs cost $10.80 sync or $6.00 batched, so a 10× multiplier is noise, and the cost argument only becomes material above ~600 runs/month. **Second, the 10× figure is probably overstated:** the $0.18/run is already ~90% optimised using levers that are provider-specific here — prompt caching at −90% on the globally-cached curated library (the central lever per the cost model) and Batch at −50% — plus 7 of 10 agents already on the cheapest tier. The honest comparison is optimised-vs-optimised, and it hinges on whether DeepSeek offers equivalent caching and batch economics. **Beyond cost, the axis weighted second only to quality is data jurisdiction:** runs send named driver telemetry to the provider, LMU's player base is heavily European, and charging EU customers puts GDPR transfer obligations on Axiom Black. That is a legal constraint, not a preference, and it may close the question regardless of price. **DeepSeek's figures are deliberately absent from the doc** — this environment's egress proxy blocks `api-docs.deepseek.com`, and a recalled number in a decision document is the exact failure mode the doc exists to prevent. Model choice stays behind `RunConfig`, which is what makes deferring a real option rather than a delay. |
| 21 Aug 2026 | **Milestone** | **Phase 1 closed — S7 done, all eight stories complete.** Owner confirmed the full acceptance test on production: the demo session's reconciliation flags render, sign-out/sign-in preserves both sessions, and leaked-password protection is on (clearing the last advisor WARN). Retrospective written to `docs/phase-1-retrospective.md`: 34 commits over 13 days, 146 frontend tests across 11 files, 6 CI rings, ~4,200 lines of non-test source, $0/month. The retro's uncomfortable finding is worth repeating here: **six defects reached `main` or production, and every one shares the same shape — a check passed in a friendlier environment than the one that mattered.** The two highest-yield debugging sessions of the whole phase were running the owner's real export through the ingest, and querying the production database after the walkthrough; each found real bugs in minutes that the full suite had missed for days. |
| 21 Aug 2026 | Decision | **Shareable read-only session link deferred, not built.** It was the other half of the §6 pull, and it cannot be built without amending a standing bar: §4 requires tenant isolation keyed on `auth.uid()`, and a public link is by definition session data readable without it. Per the Codex amendment protocol that needs explicit owner sign-off rather than quietly widening the security boundary. Owner chose to defer — there are no users to share with yet, so the link has nobody to reach, and closing the iteration cleanly is worth more than a feature with no audience. The design is recorded for when it returns: unguessable revocable `share_token` (not a boolean `public` flag), a tightly-audited `SECURITY DEFINER` RPC returning one session and its laps and nothing else, a short-lived signed URL for the trace so Storage stays private, the driver name **excluded** from the shared payload, and Ring 3 gates asserting that a wrong or revoked token returns zero rows. |
| 21 Aug 2026 | Note | **Backlog correction for the next session.** `docs/phase-1-retrospective.md` names five things carried forward; the load-bearing one is that **the committed fixture is single-lap and structurally cannot exercise multi-lap logic** — it hid both the out-lap bug and the lap-reconciliation bug. A sanitized multi-lap fixture is the highest-value test investment available and is **not currently in §6**. It should be pulled before anything that reasons about laps is built. |
| 21 Aug 2026 | Decision | **The free tier's two failure modes are now handled as expected operating conditions, not bugs** (§6 "Supabase free-tier quota policy", pulled). Every error surface previously rendered `err.message` raw, so a paused project and a full bucket both reached the driver as `TypeError: Failed to fetch` — indistinguishable from the product being broken. `lib/serviceHealth.js` classifies faults into offline / unavailable / storage-full / session-expired / permission / unknown, and `FaultNotice` renders a title, an explanation, and a **Try again** button *only* where retrying is actually worth the driver's time. **Detection limit stated rather than papered over:** a paused project, a network drop and a platform outage are indistinguishable from the browser, so the copy says "may be waking from idle" and never asserts "your project is paused". Verified by rendering all five fault states. Also added `.github/workflows/keepalive.yml` — a twice-weekly REST ping, since API requests reset the inactivity timer; it **skips with a warning** rather than failing when its two repo secrets are absent, so an unconfigured repo does not carry a permanently red workflow. The keepalive is best-effort (Supabase does not document which signals reset the timer); the graceful degradation is the real safety net. **Deliberately not built:** quota enforcement or per-driver caps — with a single-digit user count that is infrastructure with no user-visible capability attached (§4). The honest failure message was the increment worth shipping. 146 tests. |
| 21 Aug 2026 | Note | **S7 and the iteration's acceptance test are fully passed.** Owner confirmed on production: the demo session's two reconciliation flags render, sign-out/sign-in preserves both sessions, and leaked-password protection is enabled (clearing the last advisor WARN). Sentry remains deliberately off — it is wired and inert, and turning it on costs 156 kB gzipped for observability the owner does not need while they can talk to every user directly. Outstanding owner action: delete the real-COTA session so production holds no driver PII (decision made, execution pending — the Supabase connector dropped before it could run). |
| 21 Aug 2026 | **Finding** | **The seeded demo session was advertising a fastest lap that does not exist — every new account saw it.** Found by querying the production database after the owner's acceptance run, not by any failing test. `sessions` claimed `lap_count 3` / `fastest_lap_no 2` / `fastest_lap_s 135.475`, while `laps` held exactly **one** row: `lap_no 0, kind partial, lap_time_s null`. Cause: `lap_count` and `fastest_lap_*` come from the `.ldx` summary, laps come from segmenting the `.ld`, and the committed fixture's `.ld` is truncated to 300 samples while its `.ldx` is the full original — so the summary describes a session the trace cannot support. Worse than cosmetic: the S5 schema derives "is this the best lap?" as `lap.lap_no === session.fastest_lap_no`, which on the demo matches nothing and explains nothing, and `SessionReport` opened the lap picker on the same phantom lap 2. Second, milder case on the real upload: `.ldx` says 135.475 s, beacon segmentation computes 135.500 s for the same lap — the header and the lap row contradicted each other by 25 ms. |
| 21 Aug 2026 | Decision | **`.ldx` wins for lap summary; `.ld` wins for lap boundaries; disagreement is flagged, never reconciled silently.** Owner chose this over making the `.ld` authoritative — it keeps MoTeC's precision, which is the entire reason `.ldx` is the preferred source, while honouring the §4 bar that unreliable data is flagged rather than hidden. Implemented as a pure, schema-free module (`lib/lapReconciliation.js`) computing flags on read from already-persisted values, so **no migration and no re-ingest** — existing sessions gain the flags immediately. Sub-sample drift is deliberately *not* flagged (`DRIFT_TOLERANCE_S = 0.05`): at 25 ms it is below one sample period and would fire on nearly every upload, so instead the `.ldx` time is displayed for the fastest lap and the visible contradiction disappears. Unsupportable summaries **are** flagged, in the UI, with both numbers named. `isFastestLap()` replaces the bare lap-number comparison and matches only laps that exist *and* are timed — a summary pointing at an out-lap is as unsupported as one pointing at nothing. 19 new tests built from the actual production rows; 128 total. Verified by rendering both sessions in a harness: the demo shows two flags, opens on its real lap 0, and stars nothing; the real session shows no flags, opens on lap 2, and its lap row now reads 2:15.475 in agreement with the header. |
| 17 Aug 2026 | Progress | **The open-signup exposure is closed.** Owner added `https://bytecraft-racing.vercel.app` to Supabase → Auth → URL Configuration and re-enabled **Confirm email**, in that order (reversing them would have sent confirmation links to localhost). The window is worth recording plainly: confirmation was disabled 12 Aug for test-signup friction, the pilot went public 17 Aug, and for that interval anyone holding the URL could create a working account. Nothing indicates it was found — the tables were empty at audit — but the exposure was real and was caused by a *temporary* convenience that no one owned turning back off. Standing rule now in `docs/deploy.md`: a setting relaxed for a private demo is not allowed to survive the deployment going public. |
| 17 Aug 2026 | Note | **The duplicate `Ring 4` required-check entry is being left as-is.** The API reports 6 entries / 5 unique on ruleset `main-gates`; GitHub's UI collapses identical rows, so there is nothing for the owner to click. It is functionally inert — requiring the same check twice is identical to requiring it once, and all five rings are enforced. Claude cannot correct it either: `PATCH /repos/…/rulesets/…` returns 403 *"Write access to this GitHub API path is not permitted through this proxy"*, the same sandbox restriction that blocked creating the ruleset. Closed as won't-fix rather than left as a standing chore. If it ever needs cleaning: remove and re-add the five checks in the UI. |
| 17 Aug 2026 | Decision | **Sentry is opt-in and lazily loaded, because a static import costs more than the product.** `import * as Sentry from '@sentry/react'` took the bundle from **461.64 kB → 938.82 kB** (132 → 289 kB gzipped) — the SDK is bigger than the app, and that weight would be paid on first paint by every driver whether monitoring was configured or not. Moved behind a dynamic `import()`: with no `VITE_SENTRY_DSN` the chunk is **not even emitted** (the import is statically unreachable and gets eliminated), and with a DSN the entry chunk stays 466 kB while Sentry loads out of band as a separate ~156 kB gzipped chunk. Accepted trade-off, stated in `docs/deploy.md` rather than buried: errors during the load window are buffered and flushed, but an error that kills the page inside that window can still be lost — full first-byte fidelity is not worth 156 kB gzipped on every visit. Deliberately **not** added to the Ring 5 build guard: a missing DSN degrades observability, it does not break the product. `@sentry/react` is MIT (checked before adding, per the licence bar). |
| 17 Aug 2026 | Decision | **Monitoring is built around the fact that this app holds driver PII.** A real name sits in the `.ld` header, an email is the login, and every Storage path carries an `auth.uid()` prefix — an error reporter is by construction a pipe shipping application state to a third party. So: **no Session Replay** (it would record the driver's screen, name included), `sendDefaultPii: false`, emails/UUIDs/JWTs redacted from every outgoing string (a leaked Supabase access token is a live credential), request bodies dropped wholesale (for this app a body *is* a telemetry upload), and breadcrumbs naming a `.ld`/`.ldx`/`.svm` discarded outright since MoTeC exports are typically saved as `<driver name> <track>.ld`. **Limitation recorded rather than implied:** redaction is a denylist over known shapes and cannot catch an arbitrary driver name in an arbitrary error string. The Sentry project should be treated as holding potentially-identifying data, with a short access list. 18 tests, weighted toward what must *not* escape. |
| 17 Aug 2026 | **Risk** | **G5.2 was picking its bundle positionally, and lazy chunks broke that assumption.** The gate resolved its target with `find dist/assets -name '*.js' \| head -1`, which was unambiguous only while the build emitted exactly one chunk. Configuring a Sentry DSN emits a second chunk that is *larger* than the app and contains **no** app code — so the gate could have asserted against the wrong file and failed for entirely the wrong reason. Now resolves the **entry** chunk from `dist/index.html` and fails loudly if that reference is missing. Verified in both configurations (DSN set and unset). The general shape: a check that identifies its subject by position rather than by name is a check that silently changes meaning when the output shape changes. |
| 17 Aug 2026 | Correction | **`docs/deploy.md` was advising the exact misconfiguration that is currently live.** It recommended turning Supabase "Confirm email" **off** for frictionless demo signups — sound guidance for a private walkthrough, and actively harmful now the deployment is publicly reachable, where it means anyone with the URL gets a working account. Rewritten to make the trade-off and the ordering explicit (redirect URL first, or the confirmation emails point at localhost), and annotated with what actually happened: disabled 12 Aug for test friction, still off when the pilot went public on 17 Aug. Documentation written for a private demo does not silently stay correct once the thing is public. |
| 17 Aug 2026 | Progress | **Integrated the parallel session's cloud-bootstrap work onto current `main`.** A second session had produced three commits on `claude/race-engineer-cloud-migration-q13f8j` that were stranded behind PR #11 and #12. Cherry-picked all three onto `main` on a fresh branch rather than rebasing theirs — their branch is left untouched, so nothing is destroyed if that session is still live (the 12 Aug collision lesson). Conflicts were confined to `WORKING_PLAN.md` and `docs/build-breadcrumbs.md`, both append-only tables colliding at the same line; every conflict was resolved by **keeping both entries**, never by choosing a side. Verified their work rather than trusting it: `scripts/cloud_setup.sh` runs clean and exits 0, the rolldown guard loads, and `frontend/.env.local` is gitignored by `*.local` in both the root and `frontend/` ignore files — so the env bridge cannot leak a key into git. **One caveat on their premise:** this session's environment exposes **no** `NEXT_PUBLIC_SUPABASE_*` variables, so the bridge is a no-op here. It degrades silently and correctly rather than failing, but the claim "the cloud environment supplies them" holds for *their* environment, not for cloud sessions generally. |
| 17 Aug 2026 | Correction | **§0's Health and Top-blocker rows were stale for one commit.** PR #12 added the Live URL row and moved S7 to partial, but left Health claiming S7 "cannot start until Vercel can see the repo" and Top blocker naming the GitHub login connection plus four branches that had already been deleted — all of it false by the time it merged. Fixed here. The tracker's whole value is being true at a glance; a status block that contradicts the row three lines below it is worse than no status block. Update the summary rows in the same edit as the detail rows, not as a follow-up. |
| 17 Aug 2026 | Progress | **The pilot is deployed — and the first production build proved the point of Ring 5.** PR #11 merged as `49a7a78`; five rings green on `main`, Vercel production deployment completed. **What happened first is the lesson:** Vercel's initial production build ran from `main` *before* the merge, so it had no build guard and no env vars — and served an asset the owner measured at **0.1 kB**. The branch preview, built from the same source *with* the guard and the vars, loaded correctly. Two builds of nominally the same product, one blank, and nothing in the green build log distinguished them. Ring 5 only protects what it has been merged into; a guard on a branch protects nothing in production. Diagnosis was slower than it should have been because the sandbox proxy blocks `vercel.app`, so I could not fetch the deployed artifact at all and had to reason from the owner's observations — worth remembering that **the deploy target is outside this environment's reach**, which makes the CI-side artifact assertion (G5.2) the only check I can run myself. |
| 17 Aug 2026 | Progress | **Cloud-session bootstrap committed to the repo.** Work moved to the `Race Engineer - Dev` cloud environment, where every session clones the repo into a fresh container — no `node_modules`, so Vitest and oxlint could not run until someone installed deps by hand. Added `.claude/settings.json` (a `SessionStart` hook on `startup\|resume`) and `scripts/cloud_setup.sh`, which installs `frontend/` deps when `CLAUDE_CODE_REMOTE=true` and is a no-op otherwise. Two corrections to the obvious version: the npm project is `frontend/`, not the repo root (there is no root `package.json`, so a root `npm ci` installs nothing), and the script repeats CI's `require('rolldown')` guard for the npm optional-deps bug (npm/cli#4828) that otherwise breaks Vitest. The backend's Python deps are deliberately **not** installed by the hook — `pip install -e ".[dev]"` pulls numpy/pandas/scipy/anthropic and would tax every session start; install by hand for the Ring 1 suite. Verified end to end in this container: hook 3.9 s, 57/57 Vitest green afterwards, and both off-cloud paths (`CLAUDE_CODE_REMOTE=false` and unset) exit 0 without touching anything. `CLAUDE.md` gained a **Commands** table (frontend/backend/RLS commands mapped to their rings, with `.github/workflows/ci.yml` named as the authority on conflict) and a **Branch rules** section carrying `CONTRIBUTING.md`'s load-bearing rules inline. |
| 17 Aug 2026 | Progress | **Cloud-session bootstrap committed to the repo.** Work moved to the `Race Engineer - Dev` cloud environment, where every session clones the repo into a fresh container — no `node_modules`, so Vitest and oxlint could not run until someone installed deps by hand. Added `.claude/settings.json` (a `SessionStart` hook on `startup\|resume`) and `scripts/cloud_setup.sh`, which installs `frontend/` deps when `CLAUDE_CODE_REMOTE=true` and is a no-op otherwise. Two corrections to the obvious version: the npm project is `frontend/`, not the repo root (there is no root `package.json`, so a root `npm ci` installs nothing), and the script repeats CI's `require('rolldown')` guard for the npm optional-deps bug (npm/cli#4828) that otherwise breaks Vitest. The backend's Python deps are deliberately **not** installed by the hook — `pip install -e ".[dev]"` pulls numpy/pandas/scipy/anthropic and would tax every session start; install by hand for the Ring 1 suite. Verified end to end in this container: hook ~4 s, then 91/91 Vitest, lint, and build green; both off-cloud paths (`CLAUDE_CODE_REMOTE=false` and unset) exit 0 without touching anything. `CLAUDE.md` gained a **Commands** table (frontend/backend/RLS commands mapped to their rings, with `.github/workflows/ci.yml` named as the authority on conflict) and a **Branch rules** section carrying `CONTRIBUTING.md`'s load-bearing rules inline. |
| 17 Aug 2026 | **Finding** | **Every cloud session's build was dead on arrival — the env vars are there under the wrong names.** Rebasing onto #11 surfaced it: `vite.config.js` requires `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` (Ring 5 / G5.1 refuses the build without them), but the `Race Engineer - Dev` environment injects the *correct values for the correct project* under Next.js names — `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`. Vite only exposes `VITE_`-prefixed names, so it cannot see them: `npm run build` fails and `npm run dev` boots into a runtime throw, in every cloud session, on a fresh clone. Nothing in CI catches this because CI sets its own placeholders — the gap lives *between* the environment config and the repo, which is exactly the seam no ring tests. Note the value shape was already right: the key is a `sb_publishable_…` (46 chars), pointed at `nunkznbcdxgsofqlogri`. **Fixed in `scripts/cloud_setup.sh`**, which now writes `frontend/.env.local` from whichever names are present (real `VITE_*` preferred), never clobbering an existing file; `.gitignore` already excludes `*.local`, and the key is publishable by design. Re-verified after the fix that G5.1 still has teeth — with `.env.local` removed and all four variables unset, the build still refuses. **The durable fix is owner-side**: rename the two variables in the environment config so the bridge becomes redundant. |
| 17 Aug 2026 | Decision | **Branch protection is live — S2 closes.** Repository ruleset `main-gates` (id `20923480`), active, targeting the default branch: restrict deletions, block force pushes, require a PR at 0 approvals, and require all five ring checks with strict up-to-date enforcement. Verified by reading the ruleset back, not by assuming the save worked — which surfaced a **duplicate**: `Ring 4 · JS golden-master parity (G4.1–G4.3)` is listed twice. Harmless (the same check simply required twice) and all five rings are present, so no gate is missing; worth tidying when convenient. **Correction to the record:** the earlier claim in this row that "GitHub free tier only enforces protection on public repos" was my inference from `"protected": false`, which only ever meant *no rule had been created*. It was not verified and should not have been stated. The two 403s I hit probing this were my own limits — the GitHub App lacks the Administration permission, and the sandbox proxy blocks admin writes outright — neither of which says anything about the account's plan. |
| 17 Aug 2026 | **Risk** | **Required status checks match on the literal job name.** Rename a CI job and its required check stops matching — enforcement silently drops that gate while everything still shows green. Two job titles were renamed this session (Ring 3 gained G3.5/G3.6, and Ring 5 was added), so this is a live hazard, not a theoretical one. Rule: renaming a job and updating the ruleset are one change, never two. |
| 17 Aug 2026 | **Finding** | **The storage RLS policies could not support the upload retry path.** Audited the live Supabase project (`nunkznbcdxgsofqlogri`) against the client code — not prompted by any failing test. The Phase 1 migration granted SELECT/INSERT/DELETE on `storage.objects`; all four uploads in `lib/sessions.js` pass `{ upsert: true }`, and an upsert onto an existing object is an **UPDATE**. So a retry after a partially-failed upload — precisely what `upsert: true` was added to fix — died with an RLS violation. `rollbackSession()` hid it most of the time by deleting orphans, but it cannot run when the failure is a closed tab, a dropped connection, or a crash mid-upload, which are the cases a retry exists for. Fixed by `20260817010000_storage_update_policy.sql`, scoped identically to the existing three (own `auth.uid()` prefix only — no tenant boundary widened). **Applied to the live project** and verified: policy present, security advisors still return zero lints. Also confirmed live schema matches the repo migrations column-for-column, `three_file_atomicity` and the `(user_id, ld_sha256)` dedup index are present, the `telemetry` bucket is private, and both tables are empty (0 rows). |
| 17 Aug 2026 | Decision | **Ring 3 gains G3.5/G3.6, and the shim gained `updated_at`.** G3.5 asserts a driver *can* overwrite their own object; G3.6 asserts the same UPDATE affects **0 rows** against another driver's — an UPDATE policy is the easiest place to widen isolation by accident, so the negative case is asserted alongside the positive. The `00_auth_shim.sql` `storage.objects` table gained `updated_at` (real Supabase has it) so the test exercises the UPDATE the way the client actually does rather than against a column invented for the test. **Both gates were verified to have teeth**: with the fix migration omitted the suite exits 3 on G3.5; with it applied all seven checks pass. Ring 3 now runs 7 assertions, up from 5. |
| 17 Aug 2026 | Note | **Migration version drift, harmless but worth knowing.** The live migration history records `20260812024930_s5_trace_and_demo_schema` while the repo file is `20260812030000_s5_trace_and_demo_schema.sql` — same content, ~31 min apart in stamp, from the migration being applied before the file was finalised. Left alone deliberately: renaming the repo file to match would rewrite history for no behavioural gain, and the schema was verified column-for-column identical. Flagged so a future session doesn't read it as an unapplied migration. |
| 17 Aug 2026 | Progress | **Rendered S6 in an isolated harness — 33 passing logic tests had missed two defects.** Stubbed `lib/sessions` and `lib/auth` behind regex Vite aliases and drove the real component with Playwright (recipe in `docs/build-breadcrumbs.md` Part C). Confirmed working: car chips derived from data, filter narrowing, thresholds surviving a reload from `localStorage`, the demo session staying out of the stats, and sparklines appearing only for combos with ≥2 sessions. **Two defects found.** (1) **Every first upload was awarded ELITE.** The gap is measured against the driver's own best, so a single-session combo has a gap of exactly 0 by definition and swept the top tier — a badge earned by having nothing to compare against, which is precisely the no-faked-capability bar. `tierNameFor` now takes the run count and returns UNRANKED below two sessions; test asserts both halves (1 session → UNRANKED, 2 sessions at gap 0 → ELITE, since matching a PB *is* meaningful once there is a PB). (2) **Two-session sparklines rendered as full-width slabs** — `flex: 1` alone (the prototype's rule) only looks right at higher session counts; capped bar width at 34 px. The first was invisible in text assertions and the second was invisible everywhere except the screenshot. |
| 17 Aug 2026 | **Blocker** | **S7 cannot start: Vercel has no GitHub login connection.** Attempted to create the project from here (Vercel MCP connector, team Axiom Black `team_BPwh0j3cqhypnbDcG10a0LMQ`, zero existing projects). The API refused: *"Failed to link Axiom-Black/Race-Engineer. You need to add a Login Connection to your GitHub account first."* This is an account-level authorization, not a repo-visibility or permissions problem — owner-side, Vercel → Settings → Login Connections → Connect GitHub, authorizing the Axiom-Black org. Also noted: the connector exposes create/deploy/read-logs tools but **no environment-variable write tool**, so the two `VITE_SUPABASE_*` values must be set in the dashboard regardless. |
| 17 Aug 2026 | **Finding** | **A green build was shipping a bundle with no application in it.** While verifying the build ahead of S7, the output hash and byte size were *identical* before and after a substantial source change — the tell. Cause: Vite inlines `import.meta.env` at **build** time. With the Supabase vars absent, `url`/`key` fold to `undefined`, the unconditional throw at `lib/supabase.js:10` becomes statically reachable, and the minifier dead-code-eliminates the entire app behind it. Exit 0, no warning, plausible-looking artifact: **198.70 kB without the vars vs 461.64 kB with them** — 263 kB of product silently stripped. Rings 0–4 were all green throughout, because they test *source*, not the artifact. Had S7 proceeded before this was caught, the deploy would have gone green and served a blank page with nothing in the build log to explain it. This is the third instance of the same lesson now recorded in the breadcrumbs: **a passing check in a friendly environment is not evidence about the thing you actually ship.** |
| 17 Aug 2026 | Decision | **Ring 5 — Deployable artifact — added to the gate contract** (`TESTING_GATES.md` §5a), an amendment to the Ring 0–4 ladder rather than a stretch of an existing ring: G4.x is parity between implementations, and "the artifact contains the product" is a different claim. **G5.1** asserts an unconfigured build is *refused* (new env guard in `vite.config.js`, exits 1 with remediation text); **G5.2** builds with placeholder vars and asserts the bundle contains app code **and** clears a 300 kB floor — the size check catches a partial strip that a string match alone would miss. Operational corollary now documented in both the gates doc and the guard: because values are inlined at build time, changing an env var on any host requires a **rebuild**, not a restart. |
| 17 Aug 2026 | Progress | **S6 back half complete — the three remaining items closed.** (1) **Car filter** — chip row over the rolled-up combos, rendered only when there is more than one car to choose between; a stale selection (last session of that car deleted) falls back to ALL instead of showing a confusing empty list. (2) **Tier-threshold persistence** — see the decision below. (3) **Sparkline parity with the prototype** — restored the `TREND` label above the bars, per-tier colouring on the threshold inputs, the `BEYOND → FOUNDATION` column, the italic helper line, and `flex-end` alignment on the threshold row. Also extracted the rollup maths into `lib/progression.js` so it is testable without dragging in the Supabase client (importing a component transitively loads `lib/supabase.js`, which throws by design), replaced `Math.min(...bests)` with a single-pass fold (the spread form throws `RangeError` on a long enough history), and guarded a zero Developing cutoff against divide-by-zero. **90 tests** (was 57), lint and build green. |
| 17 Aug 2026 | Decision | **Tier thresholds persist to `localStorage`, not Postgres.** They are a *display* convenience — they change how a gap is labelled, never what is measured or stored — so the "tenant isolation lives in the database" bar does not reach them. A `user_preferences` table would cost a migration, an RLS policy, and another owner-side apply step to persist three numbers, against the "every increment is payable" bar. The limitation is stated rather than hidden: thresholds do **not** follow a driver to a second device, and the UI says "Saved to this browser". `lib/prefs.js` is the single seam to swap when Phase 2 brings a real preferences surface. Keys are namespaced per `user.id` so two drivers sharing a browser profile don't inherit each other's setup, and a failed write (private browsing, storage disabled — these *throw*, they don't return null) surfaces a warning instead of silently losing the edit. |
| 13 Aug 2026 | Progress | **Cleared the two remaining low-severity review items.** (1) **Session timestamps rendered in the wrong zone.** A `.ld` header carries a wall clock with no timezone; `parseRecordedAt` stores it as the UTC instant with those digits (which keeps sessions correctly *ordered* without inventing a zone), but both display sites then ran `toLocaleString()`, converting it again — a 19:32 session would read **"3:32 PM" in US Eastern**, a 4-hour error on the field a driver uses to identify their own session. It looked correct only because this container renders in UTC. Added `lib/sessionTime.js` as the single permitted formatter (always `timeZone: UTC`), wired into `SessionReport` and `SessionList`, documented the convention at the source in `parseRecordedAt`. Tests pin the wall clock, the date-boundary case, and explicitly assert the *old* behaviour would have said 3:32 PM so a regression fails loudly. (2) **The UNRELIABLE hook was undocumented dead weight.** Kept rather than deleted — the Python reference keeps the same hook, and `reliable` is part of the golden-master contract — but added `motec/reliability.test.js` as a tripwire: it fails if anyone populates the set without also confirming the UNRELIABLE badge (currently unreachable) still renders, and pins that "empty" and "unreliable" stay separate concepts. 57 tests, lint, build green. |
| 13 Aug 2026 | Progress | **Real-file acceptance run — S5 criterion PASSES, and it found a real bug.** Ran the owner's genuine COTA export (PII; kept in `/tmp`, never in the repo — verified) through the full ingest. **Parse + resample: 94 ms against the < 10 s budget**, on 29,490 samples/channel (412,850 total) vs the fixture's 300 — ~100× the depth the committed fixture could exercise. Cross-checks all pass: Ambient 29–30 °C / Track 39.3–39.7 °C (the `scale=50` decode is right on real data), Ground Speed 0–246 km/h, RPM 0–7996 against a 7,500 rev limit, 70 channels with 13 empty, GPS/track length 5.437 km vs COTA's 5.513. **First real test of the energy branch**: the supplied Ferrari 296 LMGT3 `.svm` resolves `virtual_energy`, the 488 GTE `fuel` — both correct. S8 delta on real laps closed at exactly the lap-time difference. **Bug found:** `.ld` segmentation yielded 5 laps where the `.ldx` says 3 — the out-lap surfaced as a *valid 174.3 s lap time the driver never set*, and a 5-row lap table sat under a `lap_count` of 3. This is the low-severity `valid`-flag misnomer from the 12 Aug review, promoted to real by real data (the single-lap fixture could not express it). Fixed below. |
| 13 Aug 2026 | Decision | **Lap segments are classified by position: out / timed / partial.** Only a segment bounded by two line crossings is a lap time. The first segment starts where recording began, not at a crossing (out-lap); the last has no closing crossing (partial). Both are shown in the UI, labelled, with no lap time — never hidden (they hold real trace data) and never presented as times. Extracted as `classifyLapSegments()` and unit-tested. Cross-validated on the real export (Codex I.3): 5 segments → 3 timed laps = `.ldx` Total Laps 3, fastest = lap 2 = `.ldx` Fastest Lap 2. `lengthKm` now derives from timed laps only — an out-lap includes pit exit and a partial lap is short, so neither measures a circuit; a session with no complete lap reports `null` rather than an authoritative-looking number (the committed fixture is exactly this case, and its test was updated to assert the honest contract). |
| 13 Aug 2026 | Progress | **Repo housekeeping.** `docs/` normalized to kebab-case (9 files) with every cross-reference updated across md/yml/js/jsx/sql/py — verified zero dangling references and every markdown link target resolving. Deleted `claude_DAILY_DEV_PLAN.md`: a 6 Aug auto-generated snapshot still describing the retired "Wire the spine" iteration and claiming the S1 fixture was uncommitted (closed 10 Aug) — a second, contradictory source of truth (Codex II.4/III.4). Rewrote the README repo map, which was also stale (listed `fixtures/` as "pending — S1", omitted `frontend/`, `supabase/`, `.claude/`). Added `CONTRIBUTING.md` recording layout, file-naming, branch (`feat|fix|chore|docs/<slug>`) and PR conventions, including the never-stack-on-merged-history rule that was broken twice. Branch cleanup: verified merge status **by tree content**, not `--contains` (squash merges make that always report "not merged"). 6 branches confirmed fully merged and deleted **locally**; **remote deletion is blocked** — the GitHub App can push refs but not delete them or create tags (HTTP 403), so the 6 stale remote branches need one cleanup pass by the owner. `claude/s5-core-loop` is **deliberately retained**: it holds 4 files that never reached `main` (the superseded duplicate S5 ingest + migrations, of which only the Ring 3 gate was salvaged into PR #4), and with tag creation blocked the branch itself is the only archive. |
| 13 Aug 2026 | Progress | **S8 closed — lap-vs-lap overlay + Δ-time trace.** New `frontend/src/lib/delta.js` (pure, no I/O): `cumulativeLapTime` reconstructs elapsed time per trace point by integrating `1/speed` over the normalized-distance axis and scaling so the lap totals its recorded time; `deltaTrace` then differences two laps pointwise. The pointwise subtraction is only valid because S5 resampled every lap onto the SAME distance grid — the decision paying off exactly as intended. Chosen deliberately over adding a time field to the trace: this reads **traces already in Storage**, so no re-ingest or migration. Untimed (in-progress) laps return `null` rather than a fabricated delta. UI: a "vs" reference-lap selector; every Instruments plot draws the reference dashed behind the selected lap on a shared y-scale; a Δ-time plot on top, segment-coloured green (gaining) / red (losing) with cursor + finish readouts. 16 new tests (48 total), lint + build clean; verified in-browser against a synthetic 2-lap session — finish delta −1.550 s for 138.800 vs 140.350. Also removed a stale dummy `frontend/.env.local` left over from an earlier preview harness. |
| 12 Aug 2026 | Progress | **S5 Step 4 — full SessionReport; S5 closed.** Built `frontend/src/components/SessionReport.jsx`: four tabs (Summary / Performance / Instruments / Track Map) on one lap selector + a synced distance cursor, all computed from the persisted lap summaries and the distance-resampled trace blob — no sample data. Summary = header + GPS track silhouette + stat grid + full channel inventory with EMPTY/UNRELIABLE badges + domain filter; Performance = per-lap top speed / peak lat+long G / max RPM / full-throttle share + a speed-band profile; Instruments = inline-SVG plots (speed, throttle, brake, RPM, gear, lat/long G) vs distance with a shared scrub cursor and per-plot readouts; Track Map = GPS polyline segment-colored by speed with the synced cursor dot. Deleted the `SessionDetail` stand-in (Codex III.4). Trace-absent sessions degrade gracefully. 32/32 Vitest, oxlint + build green. Also committed the **deploy kit** for the live demo: `frontend/vercel.json` (Vite framework + SPA rewrite) and `docs/deploy.md` (dashboard import, the 2 env vars, Supabase demo-readiness, smoke test) — the Vercel connect is the one step outside this environment, so it's handed to the owner rather than done here. |
| 12 Aug 2026 | Progress | **Independent review of the merged S5 + fixes.** An independent read of the merged S5 (`ingest.js`/`sessions.js`/`demo.js`/UI/migrations) against the DE Codex + standing bars found tenant isolation, the DB atomicity constraint, and EMPTY-flag *rendering* all genuinely in place — but three real defects (verified against the code, not taken on faith). Fixed on `claude/s5-fixes`: **(HIGH)** `uploadSession` left a `pending` row holding the `(user_id, ld_sha256)` dedup slot + orphaned Storage on any post-insert failure → a transient error locked the user out of that file forever ("already uploaded") and bricked new accounts whose demo seed half-failed; now rolls back (delete row → cascade-drops laps; remove objects) and uploads with `upsert`. **(MED-HIGH)** `Math.min/max(...samples)` overflowed the call stack on channels past ~100k samples → a real multi-lap export crashed the whole upload (the truncated fixture hid it); replaced with a single-pass `extent()` + a 500k-array regression test. **(MED)** the seeded demo session polluted real Progression stats; now excluded from gap/trend/best (still listed). Plus a one-line stale migration cross-reference. 32/32 Vitest, lint + build green. Lower-severity notes (the `valid` lap flag naming, dead `UNRELIABLE` path, `.ld` local-time-as-UTC) left documented for later. |
| 12 Aug 2026 | Progress | **Ring 3 auth/tenancy gate automated.** `main` had the pilot schema + RLS but no automated Ring 3 check (CI still said RLS is "enforced in the Supabase project itself and verified per-change" — i.e. by hand). Added `supabase/tests/00_auth_shim.sql` (recreates Supabase's `auth` + `storage` schema/roles on a bare Postgres so the real migrations apply unmodified), `supabase/tests/01_rls_acceptance.sql`, and a `ring3-auth-tenancy` CI job on an ephemeral `postgres:16` that applies the committed migrations in filename order and asserts G3.1/G3.2/G3.4: cross-user read = 0 rows across `sessions`, `laps`, **and** `storage.objects`; spoofed `user_id` insert rejected; incomplete-set `complete` rejected; duplicate `.ld` rejected; cross-folder storage write rejected. Verified locally on PostgreSQL 16 against the real migrations. `TESTING_GATES` Ring 3 is now an enforced gate, not a manual note. *(This is the one surviving piece of the never-merged `claude/s5-core-loop` spike — its Step-1 migration and Step-2 ingest duplicated the S5 work another session merged to `main`, so they were dropped; only the Ring 3 harness carried over, adapted to `main`'s schema. Re-verify against current ground truth — Codex VI.6.)* |
| 12 Aug 2026 | Progress | **Real navigation shell built** — the app had zero tabs before this (Tosin: "I didn't see the rest of the tabs" — accurate; `AppShell` only ever swapped list/upload/detail views, no persistent nav). Per direction to reference `RaceEngineeringAgent_v2.jsx`, `ByteCraft_v12_Merged.jsx`, and `ByteCraft_SessionUpload.jsx` for the workflow: added `TabBar.jsx` (SESSIONS / PROGRESSION / RACE ENGINEER / LIBRARIES, matching v12_Merged's `DriverApp` tab structure), extracted the existing view-state machine into `SessionsTab.jsx`, and shipped `ProgressionTab.jsx` (S6 front half — see its row above). Race Engineer and Libraries are **genuinely disabled** (`PhasePlaceholder.jsx`) — they explain what's coming and why it's dark (agent domains named from `RaceEngineeringAgent_v2.jsx`'s `DOMAIN_AGENTS`, library names from `v12_Merged`'s Libraries tab) rather than faking function, per the standing bar and CLAUDE.md's "resist shipping the agent early." Also ported `SessionUpload.jsx`'s actual interaction pattern into `UploadDropzone.jsx` (single drag-and-drop target + 3 status cards + pending→parsing→complete/failed pipeline indicator, replacing the plainer three-slot-only version) and added domain-color-coded filter chips to `SessionDetail.jsx`'s channel inventory (`Telemetry/Tire/Brakes/Aero/Powertrain/Environment/GPS/Session`, same 8 domains `lib/motec/domain.js` already classifies — the color map lives in the component, not the parser module, to keep `lib/motec/` theme-agnostic). Live-verified in-browser end to end: all 4 tabs render and switch correctly, Progression computes real gap/trend/tier from the persisted demo session, channel domain filtering works, zero console errors on a clean tab. Cleaned up after. Build/lint/29 tests still green. |
| 12 Aug 2026 | Decision | **Two parallel S5-back-half designs collided; reconciled onto `docs/s5-implementation-plan.md`'s architecture.** A "locked" plan doc landed on `main` (PR #2) describing a different back-half design than what had already been built independently in this session: distance-based lap resampling vs time-based, one `trace.json` Storage blob per session vs traces inline in `laps.trace` jsonb, and a different `sessions`/`laps` field-name contract. Reconciliation call (not a coin-flip — see the product-fit ranking that preceded this): the plan doc's two substantive decisions are objectively better fits for *this* product regardless of which side got there first — distance alignment is a hard prerequisite for S6 (cross-session Track Map) and S8 (lap-vs-lap delta overlay) already committed on this same story list; Storage-blob traces avoid bloating the free-tier 500 MB Postgres cap already flagged in §6. Executed: rewired `frontend/src/lib/ingest.js` to integrate Ground-Speed·dt into cumulative track distance and resample each lap to ~400 points along normalized distance (not time), added wheel-slip-ratio and session-wide-GPS-bounding-box normalization to match the plan's frozen `pts` shape; `frontend/src/lib/sessions.js` now uploads one `trace.json` blob per session instead of storing traces in Postgres. Schema converged via a new migration (`supabase/migrations/20260812030000_s5_trace_and_demo_schema.sql`) — also retroactively committed the S4 schema as `20260810035850_phase1_schema_rls_storage.sql`, closing the "pilot schema uncommitted" gap the plan doc itself flagged (it didn't know a schema was already live; a fresh `CREATE` would have conflicted — this ALTERs instead). Deliberate deviations from the plan doc, and why, are logged inline in `docs/s5-implementation-plan.md` next to each step (kept `laps.user_id` + its own RLS policy over a join-based one, since it was already DB-layer-verified; kept `setup`/`session_type`/`energy_scheme` the plan didn't list, since dropping them regresses standing bars; `is_best` now derived client-side, not stored). Everything re-verified live against the real Supabase project after reconciliation: RLS + extended atomicity constraint (now requires `trace_path`) re-checked at the DB layer; full parse→dedup→upload→laps→complete→read-back path exercised end-to-end via a temporary script (real sign-in, real fixture, real rows — cleaned up after); demo-session seeding verified in the actual browser against a fresh zero-session account, including the trace blob round-tripping through Storage. All test rows/files/users removed after each check — nothing test-related was left in the shared project. `frontend/src/lib/demo.js` (new) + a second copy of the sanitized fixture in `frontend/public/fixtures/` (for the browser's `fetch()`; `.gitignore` extended to allow it) implement demo seeding. `docs/s5-implementation-plan.md` Steps 1/2/5 marked done with reconciliation notes; Steps 3 (upload-flow polish) and 4 (full tabbed SessionReport) remain open — `SessionList.jsx`/`SessionDetail.jsx`/`UploadDropzone.jsx` built this session are a deliberately minimal, honestly-labeled stand-in, not that dashboard. |
| 12 Aug 2026 | Decision | **Confirm-email disabled for the pilot Supabase project — approved by Tosin, tied to S8.** Free-tier SMTP's low send-rate-limit was blocking test signups (hit twice this session). No MCP tool exposes Supabase Auth config, so this needs a manual dashboard toggle: **Authentication → Sign In / Providers → Email → disable "Confirm email"** (`nunkznbcdxgsofqlogri` project). **⏰ REMINDER — re-enable before/at S8 completion**, since RLS (not email confirmation) is the real security boundary only while there's no real user base yet; flip it back on once S8 closes and the pilot nears real users. Also flagged in S8's row and §6. |
| 11 Aug 2026 | Progress | **S5 front half — onboarding spine shipped.** Replaced the S4 smoke screen with the real onboarding funnel: `AuthProvider`/`useAuth` over Supabase Auth (`src/lib/auth.jsx`), email+password sign-up / sign-in / sign-out, an auth-gated root (`App.jsx` `Gate`) with a first-paint loading guard so a restored session never flashes the sign-in form, a branded `AuthScreen` (client-side validation + friendly Supabase error mapping; treats a session-less sign-up as "confirm your email"), and an `AppShell` with top bar + honest empty-state dashboard pointing at the next step (upload is disabled, not faked — Standing bar: no faked capability). Shared tokens in `src/theme.js`, atoms in `src/components/ui.jsx`. Rebased onto the S3 merge; kept S3's `package.json` (`vitest` + optional-only win32 binding, which already resolves the Linux `EBADPLATFORM` install). `npm run build`, `oxlint`, and `vitest` (18/18) all green on Linux. |
| 10 Aug 2026 | Progress | **S3 closed.** JS parsers ported from the Python reference into `frontend/src/lib/motec/`: full decode formula (`shift` + `scale`), latin1 strings, truncation clamp, `all_zero` flags, lap boundaries from Lap Number. Vitest golden-master suite 18/18 against the fixture — includes G1.5 tripwires (scale 50/50/9, int32-only GPS, `0x9E`) and G1.4 truncated-input safety. G4.2 domain mapping snapshot generated and committed (`fixtures/golden_master_domains.json` — frozen verbatim from the prototype `domainOf`). Ring 4 added to CI as a third job after Ring 1 (with a guard for the npm optional-deps bug that also bit locally — win32 rolldown binding moved to `optionalDependencies`). The prototype JSX parser and its `CAL` badges are now formally superseded. |
| 10 Aug 2026 | Progress | **S4 closed; S2 CI committed.** Supabase project `bytecraft-racing` created (us-east-1, free tier, cost confirmed $0/mo). Phase 1 migration applied: `sessions`/`laps` with RLS on `auth.uid()`, `three_file_atomicity` check, `(user_id, ld_sha256)` dedup, private `telemetry` bucket with per-user folder policies. Acceptance verified at the DB layer (cross-user read 0 rows; spoofed insert rejected; incomplete-set `complete` rejected) and from the browser (Vite scaffold smoke screen: reachable/auth/RLS all green). `.github/workflows/ci.yml` added — Ring 0 → Ring 1 on every push/PR to `main`. Branch protection remains manual and is plan-gated on private repos (GitHub free enforces only on public). Frontend scaffold note: Vite 8 uses rolldown; the npm optional-deps bug required force-installing `@rolldown/binding-win32-x64-msvc@1.2.3` locally. |
| 10 Aug 2026 | Progress | **Reference bundle reviewed and merged to `main`.** Manual gate pass (no CI yet): bundle based on current `main`; fixture hashes match `FIXTURE_NOTES.md`; `DRIVER_REDACTED` confirmed at `0x9E` by byte inspection; repo-wide PII grep clean; all deps MIT/BSD/Apache; our adapter shims untouched. Python suite run against the fixture: **124 passed / 13 skipped / 0 failed** after fixing the golden-master test's stale key names (the wiring gap the 10 Aug entry below predicted). Review fixes committed on the branch before merge: test keys, a §5 log line that quoted the driver's surname inside a grep command (G0.2), and a `FIXTURE_NOTES.md` erratum. |
| 10 Aug 2026 | Decision | **Ambient/Track Temp reclassified — not an LMU quirk.** The long-standing "unreliable temperature channels" flag was masking our own decode bug: the `scale` field (0x1C) is a divisor (`phys = raw × mul / (scale × 10^dec) + shift`), 50 on both temp channels — dropping it produced the −265 °C garbage that got the channels flagged. With the full formula they decode to plausible values (~29/39 °C) and the parser no longer flags them. §4 standing bar, `TESTING_GATES.md` G1.1/G1.3/G1.5, and `CLAUDE.md` updated; JS port (S3) must implement **both** `shift` and `scale`. |
| 10 Aug 2026 | Progress | **S1 closed.** Reference bundle received and committed on branch `claude/materials-repo-guide-wy7pl4` (PR pending review by Tosin): FastAPI scaffold under `backend/app/` + `backend/tests/unit/`; sanitized `.ld`/`.ldx`/`.svm` triple + split golden-master JSON (`golden_master_ld/ldx/svm.json`) under `fixtures/`; `fixtures/FIXTURE_NOTES.md` records the SHA-256 hashes and closes all three open decode questions. All three resolved from real-byte evidence: driver-name field starts at `0x9E` (not `0xA0` — findings doc corrected); full formula is `phys = raw * mul / (scale * 10^dec) + shift`, `scale` non-1 for Ambient/Track Temp (50) and Steering Wheel Position (9); no datatype-category-3 channel is float32 (GPS is the only 4-byte pair, and it's int32). PII note: the driver-name field is scrubbed with a `DRIVER_REDACTED` placeholder rather than null bytes — a deviation from "zero the whole field," recorded rather than hidden. Two received files (`backend/tests/unit/test_motec_parser.py` and a duplicate `motec-ld-format.md`) carried the real driver name in plaintext and were scrubbed before commit; full-repo case-insensitive grep for the driver's name came back clean. Known wiring gaps left for CI hookup (S2/S3): `backend/tests/unit/test_motec_parser.py`'s `TestSanitizedFixture` golden-master comparison still assumes the old flat `ld_*`-prefixed, dict-keyed-by-channel-name JSON shape and needs updating to the split-file, list-of-channels shape now committed; `app/ingest/svm.py` (referenced by `test_parser_parity.py`'s adapter shim) was not present in the scaffold. |
| 9 Aug 2026 | Decision | Rings re-grounded for the pilot: Ring 3 = Supabase RLS on `auth.uid()` (was Clerk JWT); Ring 4 = JS-vs-**golden-master** parity (no Python at runtime; Python generates the committed truth data once). `TESTING_GATES.md` updated to match. |
| 9 Aug 2026 | Blocker | JS prototype parser omits the `+ shift` decode term — 9 core channels (throttle, brake, clutch, steering ×2, G ×3, fuel) un-decodable client-side (`CAL` badges). Python parsers are the reference implementation. Also open, settle from evidence only: `scale` (0x1C) semantics; possible float32 channels read as int32; driver-name offset `0x9E` vs `0xA0`. Reference bundle requested (backend zip + sanitized fixture + golden masters). |
| 9 Aug 2026 | Progress | Housekeeping commit: repo reorganized (`docs/`, `docs/pm/`, `prototypes/`, `backend/tests/`), LICENSE (proprietary) + `.gitignore` (blocks `*.ld/*.ldx/*.svm` outside `fixtures/`) added, mislabeled file extensions fixed, PII scrubbed from findings doc + two prototype data blobs. Three copyrighted Pearson PDFs removed from the tree (also independently deleted on remote); they remain in commit history pending a rewrite decision. |
| 9 Aug 2026 | Progress | Repo live at `github.com/Axiom-Black/Race-Engineer`, `main` pushed (old-S2 push objective met; CI enforcement is new-S2). Handover bundle received and committed. |
| 9 Aug 2026 | Decision | **Tier 1 Pilot stack pivot:** Vercel (frontend) + Supabase (Postgres/Auth/Storage/RLS), $0/month; **no FastAPI in the pilot**; parsing client-side; summaries + ~400-pt/lap traces persist, raw files to Storage; no TimescaleDB. Clerk **superseded** by Supabase Auth. Iteration 1 ("Wire the spine") retired — it was Phase 2 work on the old stack. Supersedes conflicting notes in the Phase Plan, PM workbook, and older tracker entries. |
| 6 Aug 2026 | Progress | `TESTING_GATES.md` (Ring 0–4 promotion contract) formalized and committed, along with adapter-shim test files (`conftest.py`, `test_parser_parity.py`, `test_cost_invariants.py`). All backend-facing suites currently `skipif` — they define the contract but nothing is wired to real code yet. |
| 6 Aug 2026 | Progress | Frontend: `ByteCraft_SessionReport.jsx` unifies Session Report + Track Map into one tabbed component (Summary/Performance/Instruments/Track Map) driven by a single lap selector and synced cursor; fixes a prior bug where performance metrics were static instead of recomputing per selected lap. |
| 2 Jul 2026 | Decision | Adopted this Working Plan as the operational tracker atop Phase Plan v1.0. |
| 30 Jun 2026 | Resolved | MoTeC `.ld` decode formula (`raw × mul / 10^dec + shift`) confirmed against real files; all 70 channels in validated ranges. Launch blocker cleared. |

---

## 6 · Backlog *(parked — pull only when it becomes "Do Now")*

**Phase 1 candidates (cheap, high value — next pulls if the iteration runs ahead):**
- **Sanitized multi-lap fixture** — the committed fixture is single-lap and *structurally cannot* exercise multi-lap logic; it hid both the out-lap bug (17 Aug) and the lap-reconciliation bug (21 Aug). Highest-value test investment available. Pull before building anything that reasons about laps.
- Shareable read-only session link (public flag under RLS / signed URL) — distribution in the sim-racing Discord ecosystem.
- CSV export of decoded channels.
- Simple 3-sector splits (distance thirds per lap) feeding Progression.
- Supabase free-tier quota policy: decide behavior at the 1 GB storage cap (~1,100 raw sessions) and mitigate the free-project inactivity pause before the pilot has real users.

**Phase 2+ (unchanged):**
- Stripe billing lifecycle (quotas are fiction without it).
- Garage invite flow + pooled quota aggregation.
- Session notes: driver-written + agent-tagged, persisted per session.
- Published corner-dossier pipeline: draft → review → publish, versioned read-only (Phase 3).
- Car-level dimension on the Progression page (e.g. models within LMGT3).
- TimescaleDB hypertables (Phase 2+ migration decision, not a launch requirement).
- Local companion app: auto-sync `.ld/.ldx` first, live-coaching bridge later.
- Live on-track voice coaching — separate premium tier; **price its real-time cost apart from batch inference**.
- Second sim title (iRacing / ACC) — new ingest parser only; rest of stack is title-agnostic (Phase 4).

---

## 7 · Reusing this for the next Claude-built tool

The value here is the *shape*, not the ByteCraft content. To start a new tool:

1. Copy this file to the new repo as `WORKING_PLAN.md`. Delete the content in §0, §2, §3, §5, §6.
2. Keep §1 (The Loop) and §4 (Standing bars) verbatim — that's the reusable operating system. Edit §4's invariants to the new tool's non-negotiables.
3. Fill §0 status header, then write the **Now/Next/Later** in §2 (three columns, one phase each — resist more).
4. Slice only the *Now* column into §3 stories. Give each an acceptance test. That's the whole plan; don't pre-plan Next/Later in detail — Phase-N data decides Phase-N+1.
5. Drive it with The Loop. Update §0 and §5 every session.

**One-line spirit:** *plan the phase you're in, define done by a passing test, cut scope not quality, and write down what happened.*
